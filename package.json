{
  "name": "vector-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight sidecar dashboard for a Vector.dev instance",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w server && npm run build -w web",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "license": "MIT"
}
